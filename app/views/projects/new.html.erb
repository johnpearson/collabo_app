<%= form_for @project do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class="field">
		<p><%= f.label :name %><br />
		<%= f.text_field :name %></p>
		<p><%= f.label :content %><br />
		<%= f.text_area :content %></p>
		<p><%= f.label :inspire_file_name %><br />
		<%= f.text_field :inspire_file_name %></p>
		<p><%= f.label :inspire_file %><br />
		<%= f.file_field :inspire %></p>
	</div>
	<div class="actions">
		<%= f.submit "Submit" %>
	</div>
<% end %>
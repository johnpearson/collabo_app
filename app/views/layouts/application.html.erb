<!DOCTYPE html>
<html>
	<head>
  		<title><%= title %></title>
		<%= csrf_meta_tag %>
		<!--[if lt IE 9]>
		<script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"</script>
		<![endif]-->
  		<%= stylesheet_link_tag 'collabo', :media => 'screen' %>
  		<%= javascript_include_tag :defaults %>
  		
	</head>
	<body>
		<div class="container">
			<section class="round">
				<% flash.each do |name, msg| %>
					<%= content_tag :div, msg, :id => "flash_#{name}" %>
				<% end %>
				<table class="main">
					<tr>
						<% if user_signed_in? %>
							<%= render 'shared/user_home' %>
						<% else %>
							<%= render 'shared/home' %>
						<% end %>
					</tr>
				</table>
			</section>
		</div>
		<div class="footer">
			<%= render :partial => 'layouts/footer' %>
			<%= debug(params) if Rails.env.development? %>
		</div>
	</body>
</html>
